<TfStateManager @onInput={{fn this.onInput}}
                @onSelect={{fn this.onSelect}}
                @options={{@options}}
                @placeholder={{this.placeholder}}
                @value={{this.value}}
                {{did-insert this.filterOptions}} />
