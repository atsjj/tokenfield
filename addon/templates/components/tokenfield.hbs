<TfStateManager @isMulti={{this.isMulti}}
                @updateOptions={{fn this.updateOptions}}
                @onInput={{fn this.onInput}}
                @onSelect={{fn this.onSelect}}
                @options={{this.options}}
                @placeholder={{this.placeholder}}
                @value={{this.value}}
                {{did-insert this.filterOptions}} />
